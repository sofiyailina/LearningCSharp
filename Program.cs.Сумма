using System;

namespace Сумма
{
    class Program
    {
        static void Main()
        {
            int a = Convert.ToInt32(Console.ReadLine());
            int sum = 0;
            for (int b = 0, b <= a, b++ )
            {
                sum++;
            }
            Console.WriteLine(sum);
        }
    }
}
